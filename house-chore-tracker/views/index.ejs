<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chore Task Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 flex justify-center items-center min-h-screen">
    <div class="bg-white p-6 rounded-lg shadow-md w-full max-w-lg">
      <h2 class="text-2xl font-bold text-center mb-4">
        🏡 House Chore Task Manager
      </h2>

      <!-- Task Form -->
      <form action="/add" method="POST" class="mb-4 flex gap-2">
        <input
          type="text"
          name="task"
          placeholder="Enter task"
          required
          class="w-full px-3 py-2 border rounded-lg"
        />
        <input
          type="text"
          name="assignedTo"
          placeholder="Assign to"
          required
          class="w-40 px-3 py-2 border rounded-lg"
        />
        <button
          type="submit"
          class="bg-blue-500 text-white px-4 py-2 rounded-lg"
        >
          ➕ Add
        </button>
      </form>

      <!-- Task List -->
      <ul class="space-y-2">
        <% tasks.forEach(task => { %>
        <li
          class="flex justify-between items-center p-3 border rounded-lg <%= task.completed ? 'bg-green-100' : 'bg-white' %>"
        >
          <div>
            <strong
              class="<%= task.completed ? 'line-through text-gray-500' : '' %>"
              ><%= task.task %></strong
            >
            <span class="text-sm text-gray-500"
              >- Assigned to: <%= task.assignedTo %></span
            >
          </div>
          <form action="/toggle/<%= task._id %>" method="POST">
            <button
              type="submit"
              class="<%= task.completed ? 'bg-yellow-500' : 'bg-green-500' %> text-white px-4 py-2 rounded-lg"
            >
              <%= task.completed ? '🔄 Undo' : '✅ Done' %>
            </button>
          </form>
        </li>
        <% }) %>
      </ul>
    </div>
  </body>
</html>
